# Gradle Properties
org.gradle.jvmargs = -Xmx1G
org.gradle.parallel = true

# Mod Properties
version = 0.0.1
maven_group = io.github.ReactivityMeltdown
archives_base_name = reactivity-mod

# Dependencies are managed at gradle/libs.versions.toml
